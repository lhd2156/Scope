# atlas_geo tests

GoogleTest suites and pytest integration coverage live here. Phase 21.6 adds Python-level binding tests that import the repo-root `atlas_geo` package, which in turn loads the built `_atlas_geo` pybind11 extension from `build/python/`.
