
## Session memory flush
- User strongly prefers consistent Codex-like progress updates tied to real tool-backed work. Use concise Started / In progress / Done style updates; avoid vague status filler like generic "working on task", standalone ETA blocks, or repetitive "user input required" text unless actually needed.
- User wants Atlas work to continue automatically without waiting for more prompts. Only stop for real blockers that actually need the user.
- User explicitly prefers PowerShell-safe command chaining with semicolons (;) and does not want && used on this Windows host.
- User approves automatic Atlas-related installs/setup/validation work without asking each time, and wants sub-agents used/orchestrated rather than the lead agent doing everything directly.
- User wants a brief chat update when switching into a new concrete task/phase and generally prefers more doing, less talking.
- Atlas durable state as of 2026-03-28: Foundation is complete. Core was cleanly validated on eature/core-platform with dotnet build and dotnet test passing (2/2); validation/bookkeeping commit: 59b78dc and earlier cleanup/validation work included 859c267.
- Content is still in progress on eature/content-engine: Django manage.py check passed and task 14 was marked complete with commit 26e53ad, but broader dependency/test validation on Python 3.14 still needs continued repair and migration/test work.
- Intel is marked COMPLETE in canonical progress, but it still carries an integration-time caveat: pinned Python dependencies (notably scikit-learn==1.5.2) are not cleanly compatible with the current Python 3.14 validation environment and may need dependency refresh or a compatible Python runtime for full validation.
- Frontend remains IN_PROGRESS in canonical progress on task 5 (MapView), though meaningful page/store/component work and successful frontend build/test validation already exist in the repo.
- Heartbeat/orchestration state: canonical progress tracking is now under oundation/core/content/intel/frontend/PROGRESS.md plus root PROGRESS.md; local subagent spawning through the OpenClaw gateway at ws://127.0.0.1:18789 has been flaky/intermittent during this session.
