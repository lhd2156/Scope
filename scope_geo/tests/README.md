# scope_geo tests

GoogleTest suites and pytest integration coverage live here. Phase 21.6 adds Python-level binding tests that import the repo-root `scope_geo` package, which in turn loads the built `_scope_geo` pybind11 extension from `build/python/`.
