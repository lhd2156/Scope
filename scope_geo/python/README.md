# scope_geo python bindings

`scope_geo_bindings.cpp` builds the `_scope_geo` pybind11 extension into `build/python/`.
Import the repo-root `scope_geo` package to load that native module during Python integration tests and later Intel service wiring.
