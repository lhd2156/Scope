fastapi>=0.115.0
uvicorn[standard]>=0.34.0
langchain>=0.3.0
langchain-ollama>=0.3.0
langchain-community>=0.3.0
langchain-chroma>=0.2.0
# ChromaDB 1.x currently has an unfixed pre-auth code-injection advisory.
# Stay on the 0.x line until a patched 1.x release is available.
chromadb>=0.6.0,<1.0.0
# ChromaDB 0.6.x still calls PostHog's legacy capture(distinct_id, event,
# properties) API. PostHog 6+ removed that signature and logs errors even when
# Chroma anonymized telemetry is disabled.
posthog>=5.4.0,<6.0.0
pydantic>=2.10.0
pydantic-settings>=2.10.1
httpx>=0.28.0
confluent-kafka>=2.13.2
PyJWT>=2.10.0
redis>=5.0.0
python-json-logger>=3.2.0
pytest>=8.3.0
pytest-asyncio>=0.25.0
sentry-sdk[fastapi]>=2.22.0
