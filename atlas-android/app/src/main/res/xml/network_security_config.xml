<?xml version="1.0" encoding="utf-8"?>
<!--
    Scoped cleartext policy:
    - Production traffic (app stores, real devices) must be HTTPS — the default.
    - Dev traffic from the emulator hits the host machine via 10.0.2.2 and, for
      USB/Wi-Fi test devices, localhost. Both are allowed here.

    This is intentionally narrower than a blanket `usesCleartextTraffic="true"`.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
